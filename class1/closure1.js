function wait(message, interval) {
    setTimeout(function() {
        console.log(message);
    }, interval);
}

wait("Its pulse influences the assumed grave before the specialist.", 5000);
wait("The change boils.", 2000);
wait("The puzzled blade disputes the lad.", 4000);